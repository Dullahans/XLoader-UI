/**
 * 注意：
 * 当前 UI 以 `public/index.html` 的 preview 原型页为准（里面自带 Vue + ElementUI CDN + new Vue(...)）。
 * 因此这里不再挂载 Vue CLI 的 App，避免覆盖 preview 的 DOM。
 *
 * 这个入口文件保留为“占位”，确保 Vue CLI 构建流程正常。
 */
// eslint-disable-next-line no-console
console.log('[loader-ui] preview mode: UI is served from public/index.html');




